.VPDoc {
  div.docs.line,
  dl dd > div.docs {
    display: contents;

    > p {
      display: contents;
      color: hsl(
        var(--sb-fg-accent-hue) var(--sb-fg-accent-saturation)
          var(--sb-fg-lightness-normal)
      );
    }
  }

  dl.docs > dd > p {
    margin-inline-start: 1em;
  }
}
